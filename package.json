{
  "name": "mars-inc",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run index.html",
    "repl": "bun run --hot repl.ts",
    "build": "bun build index.html --outdir=docs --minify"
  }
}
